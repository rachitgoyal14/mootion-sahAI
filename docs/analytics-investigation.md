# Analytics Implementation – Live Codebase Audit

> Generated by `./inspect-analytics.sh` on $(date)

---

## 1. Backend Files (analytics-related)

$(find backend -type f -name "*.py" | grep -E "(analytics|clustering|student_actions|teachers)" | sort | sed 's/^/- /')

---

## 2. Frontend Files (analytics-related)

$(find frontend -type f \( -name "*.tsx" -o -name "*.ts" \) | grep -i analytics | sort | sed 's/^/- /')

---

## 3. Analytics Endpoints (by file)

$(grep -rn "router\\.(get|post|put|delete)" backend/app/api/ --include="*.py" | grep -i "analytics\|teachers/analytics\|students/analytics" | sed 's/^/- /')

---

## 4. Models Used for Analytics

$(grep -rn "class.*\\(Base\\)" backend/app/core/models.py | grep -E "StudentAttempt|ConceptScore|StudentTopicCluster" | sed 's/^/- /')

---

## 5. Hardcoded Mock Values (student_actions_service)

$(grep -n "streak\|prediction_accuracy\|misconception" backend/app/services/student_actions_service.py 2>/dev/null | head -20 | sed 's/^/- /')

---

## 6. Clustering Implementation

$(grep -n "KMeans\|cluster" backend/app/services/clustering_service.py 2>/dev/null | head -10 | sed 's/^/- /')

---

## 7. Duplicate / Overlapping Components

$(find frontend/src/pages -name "*Analytics*.tsx" -o -name "*Analytics*.ts" | sed 's/^/- /')

---

## 8. Response Schemas (or lack thereof)

- Schemas in \`backend/app/schemas/\`: $(ls backend/app/schemas/ | grep -i analytics || echo "None found")
- Inline schemas in endpoints: $(grep -rn "return {" backend/app/api/analytics.py backend/app/api/teachers.py | wc -l) ad-hoc dict returns

---

## 9. Notes from Code

$(grep -r "TODO\|FIXME\|HACK" backend/app/services/student_actions_service.py backend/app/api/analytics.py 2>/dev/null | head -10 | sed 's/^/- /')

